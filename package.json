{
  "name": "sunave-platform",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -ws build",
    "lint": "npm run -ws lint",
    "test": "npm run -ws test"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "pg-mem": "^3.0.14",
    "supertest": "^7.2.2",
    "vitest": "^4.1.9"
  }
}
